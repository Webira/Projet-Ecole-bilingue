{% extends 'base.html.twig' %}

{% block title %}Nos ateliers{% endblock %}

{% block body %}

    <h1><strong>Ateliers créatifs</strong></h1>

    <a class="btn btn-secondary col-md-12" style="font-size: 15px;" href="{{ path('add_eleve') }}">Inscrivez-vous aux Ateliers créatifs, ici!</a>
        <section class="cours" style="background-color: white">
          <div class="row d-flex justify-content-center text-center">
          {% for cours in courses %}

            <div class="col-md-3 m-2 mt-4 p-2 text-center" style='box-shadow: 2px 2px 4px grey'>
                <h4 class="card-header">{{ cours.titre }}</h4>
                <img src="{{ asset('images/imageCours/' ~ cours.photo) }}" class="d-block w-100" height="200" alt="image atelier">

                        <div class="card">
                            <h4 class="card-title"></h4>
                            <h4 class="card-day">{{ cours.day }}</h4>
                            <h4 class="card-duree">{{ cours.duree }}</h4>
                            <h4 class="card-age">{{ cours.age }}</h4>

                            {#  <div class="metadata">{{ cours.dateAt | date("d-m-y") }}</div>
                            {{ cours.description | raw }}#}
                            <a class="btn btn-secondary col-md-12" href="{{ path('showcours', {id: cours.id}) }}">Lire la suite</a>

                            {# <a href="{{ path('delete_cours', {id: cours.id} ) }}" style="color: red;"
                           onclick="return confirm('êtes vous sûr de vouloire supprimer le cours ?')">
                            <i class="bi bi-trash"></i></a>   {{ path('showcours', {id: cours.id}) }}#}

                         </div>
            </div>
          {% endfor %}
          </div>

        </section>


{% endblock %}

